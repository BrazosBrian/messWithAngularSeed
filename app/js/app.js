'use strict';

// Best to break up application by features instead of what is below
//
// Declare app level module which depends on filters, and services
var eventsApp = angular.module('eventsApp', []);
